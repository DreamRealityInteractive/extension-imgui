# C++ symbol in your extension
name: "DefoldImGui"

platforms:
  arm64-ios:
    context:
        defines: ["STBI_NO_THREAD_LOCALS"]

  armv7-ios:
    context:
        defines: ["STBI_NO_THREAD_LOCALS"]
